apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard
  namespace: cacheproxyfy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dashboard
  template:
    metadata:
      labels:
        app: dashboard
    spec:
      initContainers:
        - name: wait-for-proxy
          image: busybox:1.36
          command: ["sh", "-c", "until nc -z proxy 9090; do echo waiting for proxy api; sleep 2; done"]
      containers:
        - name: dashboard
          # Replace with your registry image after pushing: e.g. ghcr.io/youruser/cacheproxyfy-dashboard:latest
          # API_URL is a server-only runtime env var — no image rebuild needed when changing the proxy address.
          image: ${DOCKER_USERNAME}/cacheproxyfy-dashboard:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3000"
            - name: HOSTNAME
              value: "0.0.0.0"
            - name: API_URL
              value: "http://proxy:9090"
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
