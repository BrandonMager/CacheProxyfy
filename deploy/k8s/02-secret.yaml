apiVersion: v1
kind: Secret
metadata:
  name: cacheproxyfy-secret
  namespace: cacheproxyfy
type: Opaque
# Values are base64-encoded. To encode: echo -n 'value' | base64
# Replace all values below before deploying to a real cluster.
stringData:
  POSTGRES_PASSWORD: changeme
  CACHEPROXYFY_DATABASE_PASSWORD: changeme
  CACHEPROXYFY_REDIS_PASSWORD: ""
